import SwiftUI

enum BrandColor {
    static let background = Color("Background") // Any/Dark set
    static let card = Color("Card") // Any/Dark set
    static let primary = Color("BrandPrimary") // single color (None)
    static let accent = Color("Accent") // single color (None)
    static let success = Color("Success") // single color (None)
    static let danger = Color("Danger") // single color (None)
}
